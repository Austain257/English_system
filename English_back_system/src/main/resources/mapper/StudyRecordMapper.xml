<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.austain.mapper.StudyRecordMapper">

    <update id="markSelected" parameterType="map">
        UPDATE ${table} SET selected = 1
        WHERE id IN
        <foreach collection="selectedIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="markReviewed" parameterType="map">
        UPDATE ${table} SET already_reviewed = 1
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
